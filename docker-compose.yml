version: '3'

services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: ${POSTGRE_DB}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - ./src/main/resources/init-db/init-postgis.sql:/docker-entrypoint-initdb.d/init-postgis.sql:ro


  redis:
    image: redis:latest
    environment:
      REDIS_USER: ${REDIS_USER}
      REDIS_USER_PASSWORD: ${REDIS_USER_PASSWORD}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    ports:
      - "6380:6379"
    volumes:
      - ./redisdata:/data
    restart: unless-stopped
    tty: true


  spring-boot-app:
    build:
      context: .
    restart: on-failure
    depends_on:
      - postgres
    ports:
      - "8080:8080"
